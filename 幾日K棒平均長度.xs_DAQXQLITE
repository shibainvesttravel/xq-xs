{@type:filter|@guid:465a9075bebe4bcbb384c01b97998162}
input:Length1(10,"短日");
input:Lowerlimit(0,"下限");
input:Upperlimit(2,"上限");

settotalBar(Length1*2);

var: V1(0), V2(0);
V1 = (HIGH - LOW) / CLOSE*100 ;  { 計算當日振幅百分比 }
V2 = average(V1, length1);  { 計算 10 日平均振幅百分比 }

IF V2 < upperlimit then ret=1;  { 若 10 日平均振幅 < 2%，則 RET = 1，否則為 0 }

