{@type:filter|@guid:fcdceec424c3491483a72bdc6ba68b64}
setbarfreq("W"); // 設定時間頻率為週 (W)

input:XLength(52,"長期大量計算期數"); // 設定長期計算的週期數 (例如 52 週)
input:Length1(10,"大於幾周"); // 設定最小週數限制
input:Length2(35,"小於幾周"); // 設定最大週數限制

settotalbar(XLength); // 設定總計算範圍


value1 = FastHighestBar(volume, XLength); // 在 XLength 期間內，尋找最大成交量的週數索引值

// 如果最大成交量的發生週數在 Length1 和 Length2 之間，則返回 1
if value1 <= Length2 and value1 >= Length1 then
   ret = 1;

